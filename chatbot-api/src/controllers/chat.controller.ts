import { Controller,Get,Body } from "@nestjs/common";
@Controller("Chat")
 export class UsersController{


// POST /chat/send → Send a message to AI / generate response

// GET /chat/history → Get user’s past chat history

// DELETE /chat/history → Delete chat history (optional)

// GET /chat/:id → Get a specific chat thread

// POST /chat/save → Save chat (if not auto-saved)

// GET /chat/usage → Track number of messages used / left (for paid/free limits)
 }